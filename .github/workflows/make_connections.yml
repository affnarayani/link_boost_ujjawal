name: Make Connections

on:
  schedule:
    - cron: '31 21 * * *'
    - cron: '2 7 * * *'
    - cron: '56 3 * * *'
    - cron: '26 22 * * *'
    - cron: '53 4 * * *'
    - cron: '54 13 * * *'
    - cron: '9 18 * * *'
    - cron: '5 14 * * *'
    - cron: '58 12 * * *'
    - cron: '27 4 * * *'
    - cron: '58 14 * * *'
    - cron: '41 11 * * *'
    - cron: '25 21 * * *'
    - cron: '41 8 * * *'
    - cron: '59 8 * * *'
    - cron: '21 15 * * *'
    - cron: '57 19 * * *'
    - cron: '18 16 * * *'
  workflow_dispatch: {}

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: ci-${{ github.workflow }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  make-connections:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.PAT_TOKEN }}

      - name: Skip on 1st of month
        run: |
          if [ "$(date +%d)" = "01" ]; then
            echo "Skipping on 1st of month"
            exit 0
          fi

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Google Chrome
        uses: browser-actions/setup-chrome@v2
        with:
          chrome-version: stable

      - name: Show Chrome version
        run: chrome --version

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Playwright Browser
        run: |
          # Pehle browser install karo
          python -m playwright install chromium
          # Phir sirf un dependencies ko install karo jo ubuntu-latest (24.04) support karta hai
          sudo npx playwright install-deps chromium

      - name: Create .env with DECRYPT_KEY only
        shell: bash
        run: |
          set -e
          : > .env
          if [ -n "${{ secrets.DECRYPT_KEY }}" ]; then
            echo "DECRYPT_KEY=${{ secrets.DECRYPT_KEY }}" >> .env
          fi

      - name: Run make_connections.py (headless)
        env:
          HEADLESS: '1'
        run: |
          python make_connections.py

      - name: Commit and push changes (end only)
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if ! git diff --cached --quiet; then
            git commit -m "chore: update data from make_connections.py [skip ci]"
            if git pull --rebase --no-edit; then
              git push
            else
              git rebase --abort
              echo "Rebase failed due to conflicts, skipping push"
            fi
          else
            echo "No changes to commit."
          fi
